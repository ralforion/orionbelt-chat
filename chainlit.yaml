# chainlit.yaml
project:
  name: "OrionBelt Chat"

ui:
  # "AI Assistant" suffix = EU AI Act Art. 50(1) disclosure. Keep in sync with
  # .chainlit/config.toml [UI] name.
  name: "OrionBelt Chat – AI Assistant"
  default_collapse_content: false
  hide_cot: false  # show chain-of-thought steps (tool calls)

features:
  prompt_playground: false
  speech_to_text: false
